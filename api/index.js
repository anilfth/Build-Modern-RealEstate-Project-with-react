import express from "express";
 import mongoose from "mongoose";
 <mongoose className="conne"></mongoose>
const app = express();
app.listen(3000, () => {
    console.log('server is running on port 3000');
}
);